/* Shared directional zone colours — both FVG and OB indicators read this. */
export const ZONE_PALETTE = { demand: "#90EE90", supply: "#FFB6C1" };

/* Neutral high-contrast colour for OB pivot H/L labels (not directional). */
export const PIVOT_LABEL_COLOR = "#e8eef5";
